Read all `.md` files in `.claude/commands/` (skip this file, `obsidian-sync.md`). For each, extract the command name from the filename and write a one-line description based on its content.

Then update `/Users/matthewmannello/Documents/projects/vaults/Brain-Dos/04 Reference/AI/Claude Commands.md` by preserving the existing frontmatter and appending or replacing the body with:

```
# Claude Commands — Job Assistant

> Auto-generated. Run `/obsidian-sync` to update.

## Commands

- `/command-name` — description
- `/command-name` — description
```

Preserve all frontmatter (the `---` block at the top). Only replace content below it.

Confirm when done.
